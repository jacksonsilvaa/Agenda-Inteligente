# db

Esta pasta contém a configuração de acesso ao banco de dados.

- `session.py` → configura a conexão e a session do SQLAlchemy
- `base.py` → define a classe Base usada nos models
- Outros arquivos auxiliares podem ser adicionados conforme necessário