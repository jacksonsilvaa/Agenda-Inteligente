# models

Define os modelos (ORM) que representam as tabelas do banco de dados.

Cada modelo deve herdar de `Base` definida em `db/base.py`.

Não deve conter lógica de negócio ou consulta ao banco diretamente.